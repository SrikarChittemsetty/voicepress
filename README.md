# new-project

Starter Python project scaffold (src layout) with a tiny CLI and pytest.

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
new-project --help
pytest
```

## Layout

- `src/new_project/`: package code
- `tests/`: tests

