SECRET_KEY=change-me

# Optional: hosted Postgres URL for durable production storage on free hosts.
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Optional: absolute path to SQLite file on a persistent disk (e.g. Render).
# DATABASE_PATH=/var/data/app.db
